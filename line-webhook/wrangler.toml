name = "line-webhook"
main = "worker.js"
compatibility_date = "2024-01-01"

# KV Namespace：記錄客戶對話狀態（idle / active）
# 請先執行：npx wrangler kv:namespace create CHAT_STATE
# 然後把輸出的 id 填到下面
[[kv_namespaces]]
binding = "CHAT_STATE"
id = "5b6e49b5c7eb41eeb9cb03f11adf7144"
